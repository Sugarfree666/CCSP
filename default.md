现在有一个例子是：s1->s2->s4, s1->s3->s4这样两条从s1到s2的路径，如果用详细平衡公式:
$$
F(s_t)P(s_{t+1}|s_t)=F(s_{t+1})
$$
那么对于一条路径s1->s2->s4，就有
$$
F(s_1)P(s_2|s_1)P(s_4|s_2)=F(s_4)
$$
既然这一条流值等于了F(s4)，还有另外一条路径的流值，不就导致公式不成立了吗？



上面的思想是全局流匹配（FM）的思想，论文中为了计算可行使用的是详细平衡公式。具体地说：

什么是流匹配？
$$
\sum_{s_p \in \text{Parents}(s)} F(s_p) \cdot P_F(s | s_p) = F(s) \quad \text{（对所有 s 成立）}
$$
这是一个全局的约束，因为它要求一个状态 *s* 的流量必须等于所有父结点的流量之和。在复杂的KG中直接优化这个求和目标很困难，所以论文使用了详细平衡公式来进行局部约束，来满足另一个目标。

证明 GraphFlow 的简化目标：
$$
F(s_t) \cdot P_F(s_{t+1} | s_t) = F(s_{t+1}) \cdot P_B(s_t | s_{t+1})
$$
把DB这个等式在 s_t+1 结点的所有父节点 s_t 求和：
$$
\sum_{s_t} [F(s_t) \cdot P_F(s_{t+1} | s_t)] = \sum_{s_t} [F(s_{t+1}) \cdot P_B(s_t | s_{t+1})]
$$
也就是：
$$
\sum_{s_t} [F(s_t) \cdot P_F(s_{t+1} | s_t)]= F(s_{t+1}) \cdot \sum_{s_t} P_B(s_t | s_{t+1})
$$

$$
在一个标准的GFlowNet设置中，P_B 是一个合法的概率分布。\\
这意味着，当你处于 s 时，你必须从它的父节点中选择一个退回。\\因此，所有这些反向概率的总和必须等于 1。\\\sum_{s_t} P_B(s_t | s_{t+1})=1
$$

因此等式转化为
$$
\sum_{s_t} [F(s_t) \cdot P_F(s_{t+1} | s_t)]= F(s_{t+1})
$$
但是文中设置反向概率为1，所以产生矛盾，也就说本文主要目的并不是为了满足FM，而是满足轨迹平衡（TB）。

因此在本论文的设置下，F（s4）的值不是简单的求和，它实际是一个“势”，也就是说模型不会直接预测和计算流值，而是预测“潜力”。



​	可以对数据集进行PSE离线打分，作为Reward。在多跳问答或图检索中，模型不仅要找到可以支持回答的正确路径，这个路径还应该尽可能的合理。

1. GraphFlow 的目标是学习一个生成路径的策略，使采样出的路径分布与其质量成比例。但是reward 只使用二值或者粗略奖励信号无法区分“好的程度”
2. PSE 能告诉我们一条路径“推理得好不好”，而 GraphFlow 的 F(s) 则需要一个高质量的信号告诉它“该学哪些路径”。这两者**刚好形成互补**。









你现在是一个指导老师，现在需要指导本科生毕业设计，给你几个课题例子：
1. 传单投放地点决策系统设计与实现
2. 基于群体识别的商品推荐系统设计与实现
3. 基于社区检测算法的文献推荐系统设计与实现
4. 基于社团挖掘的音乐推荐系统设计与实现
另外给你一些包括对课题描述的例子：
1. 题目：基于LCS的Python代码查重系统设计与实现
   描述：为了减少学生作业中出现Python代码的抄袭问题,设计基于LCS的Python代码查重系统。该系统能够对学生提交的Pvthon代码进行查重,帮助教师客观地评价学生的Python代码完成情况,有效防止作业抄袭现象,从而营造良好的学风环境,促进学生的健康发展。
2. 题目：基于文本相似度的Python作业查重系统设计与实现
   描述：大学生作业抄袭是令人头疼的问题。有些学生是从网上抄袭，有些学生是抄袭往届学生作业。为了减少大学生完成作业过程中的抄袭问题,提高大学课程教学的过程性考核效果。基于文本相似度设计开发了作业查重系统。该系统以学生的python作业为训练集。利用文本相似性计算学生作业和训练集的相似性，得到作业的查重结果。
3. 题目：个性化图书推荐系统设计与实现
   描述：随着互联网行业的迅速发展，图书电商平台给消费者提供了便利的购买渠道，这促进了文化传播，如果电商平台能为消费者提供令他们满意的书籍推荐，这将会提高电商平台的销售额，实现个性化推荐目标并且能形成稳定的客户源。本课题通过使用book-crossing数据集，构建推荐系统算法模型，利用模型对用户进行top-N推荐。最终根据用户输入内容，使用算法模型给出个性化推荐结果，并在前端交互界面展示。
   你现在需要做的是：按照上述毕设题目的例子，给出5个毕设题目（题目+100字左右对该课题的描述），毕业设计要求 “算法+系统” ，你还需要确定每个题目所采用的算法，给出算法对应的论文。对应的算法应该是开源的。







你现在是一个导师，现在要指导本科毕业设计。你现在需要做的是设计一个毕业设计题目，下面会给你一个毕业设计题目的例子：

1. 题目：基于BeamQA的电影知识问答系统设计与实现 。描述：电影已成为人们娱乐生活的重要组成部分。一个专业的电影知识问答系统，能够为用户提供精准、直接且结构化的答案，极大地提升信息获取效率。本课题设计并实现了基于BeamQA框架的电影知识问答系统。该系统以DBpedia Movies 为数据基础，允许用户以自然语言提问，并即时返回准确可靠的答案。

你要给出5个毕业设计题目，要包含题目对应的100字左右的描述。毕业设计题目的领域是知识图谱与大语言模型，就像示例中的样子。可以是使用某种算法的关于某方面的问答系统。毕业设计的要求是“算法+系统”。算法对应的代码还有数据集应该是开源可获取的。另外要给出使用算法对应的论文



https://github.com/honeyandme/RAGQnASystem.git





